nodes:
  # missing maven
  # missing node
  mongodb:
    http_port: 9003
    http_user: admin
    http_password: admin
    log_level: INFO
    docker:
      name: thinking-mongodb-toskosed
      tag: "0.5"
manager:
  http_port: 10001
  http_user: admin
  http_password: admin
  docker:
    name: thinking-manager
    tag: "1.0"
